"use client";
import {useMemo,useState} from "react";
import QRCode from "qrcode";
import JSZip from "jszip";
import {PDFDocument,StandardFonts,rgb} from "pdf-lib";

type Step="start"|"setup"|"preview";
type Interaction="visit-us"|"we-visit"|"delivery"|"online";
type PlaceResult={id:string;name:string;address:string;types:string[];website:string};
const interactions:{id:Interaction;title:string;detail:string}[]=[
{id:"visit-us",title:"Customers visit us",detail:"Restaurant, salon, retail, office or clinic"},
{id:"we-visit",title:"We visit customers",detail:"Contractor, landscaper, cleaner or mobile service"},
{id:"delivery",title:"We deliver products",detail:"Delivery, ecommerce, packaged goods or takeout"},
{id:"online",title:"We work online / remotely",detail:"Consulting, digital services or virtual appointments"}];
const cats=["Home Service / Contractor","Restaurant / Food","Salon / Barber / Beauty","Retail","Healthcare / Dental","Professional Service","Online Business","Other"];
function reviewLinkStatus(v:string){try{const u=new URL(v.trim());if(u.protocol!=="https:")return"invalid";const h=u.hostname.toLowerCase();if(h==="g.page"||h.endsWith(".g.page")||h==="maps.app.goo.gl")return"good";if((h==="google.com"||h.endsWith(".google.com"))&&u.pathname.startsWith("/search"))return"search";if((h==="google.com"||h.endsWith(".google.com"))&&(u.pathname.includes("/local/writereview")||u.pathname.includes("/maps/")))return"good";return"invalid"}catch{return"invalid"}}
function validGoogle(v:string){return reviewLinkStatus(v)==="good"}
function promptFor(c:string){c=c.toLowerCase();if(c.includes("restaurant"))return"Enjoyed your visit?";if(c.includes("salon")||c.includes("barber"))return"Love your new look?";if(c.includes("home")||c.includes("contract"))return"Happy with our work?";if(c.includes("health")||c.includes("dental"))return"How was your visit?";return"Happy with your experience?"}
function categoryPhotos(c:string){c=c.toLowerCase();if(c.includes("home")||c.includes("contract"))return["https://images.unsplash.com/photo-1558904541-efa843a96f01?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1598902108854-10e335adac99?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1416879595882-3373a0480b5b?auto=format&fit=crop&w=1800&q=90"];if(c.includes("restaurant"))return["https://images.unsplash.com/photo-1517248135467-4c7edcad34c4?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1552566626-52f8b828add9?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1414235077428-338989a2e8c0?auto=format&fit=crop&w=1800&q=90"];if(c.includes("salon")||c.includes("barber"))return["https://images.unsplash.com/photo-1560066984-138dadb4c035?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1521590832167-7bcbfaa6381f?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1522337360788-8b13dee7a37e?auto=format&fit=crop&w=1800&q=90"];if(c.includes("retail"))return["https://images.unsplash.com/photo-1441986300917-64674bd600d8?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?auto=format&fit=crop&w=1800&q=90"];if(c.includes("health")||c.includes("dental"))return["https://images.unsplash.com/photo-1629909613654-28e377c37b09?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1588776814546-1ffcf47267a5?auto=format&fit=crop&w=1800&q=90"];if(c.includes("professional"))return["https://images.unsplash.com/photo-1497366754035-f200968a6e72?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1497366811353-6870744d04b2?auto=format&fit=crop&w=1800&q=90"];return["https://images.unsplash.com/photo-1497366811353-6870744d04b2?auto=format&fit=crop&w=1800&q=90","https://images.unsplash.com/photo-1497366754035-f200968a6e72?auto=format&fit=crop&w=1800&q=90"]}function categoryPhoto(c:string,i=0){const a=categoryPhotos(c);return a[i%a.length]}
function slug(v:string){return v.trim().replace(/[^a-z0-9]+/gi,"-").replace(/^-|-$/g,"")||"business"}
function download(bytes:Uint8Array|Blob,name:string,type="application/pdf"){const blob=bytes instanceof Blob?bytes:new Blob([new Uint8Array(bytes)],{type});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
function hexRgb(hex:string){hex=hex.replace("#","");return rgb(parseInt(hex.slice(0,2),16)/255,parseInt(hex.slice(2,4),16)/255,parseInt(hex.slice(4,6),16)/255)}
async function pngBytes(data:string){return Uint8Array.from(atob(data.split(",")[1]),c=>c.charCodeAt(0))}
function wrap(text:string,font:any,size:number,max:number){const words=text.split(" ");const lines:string[]=[];let line="";for(const word of words){const n=line?line+" "+word:word;if(font.widthOfTextAtSize(n,size)<=max||!line)line=n;else{lines.push(line);line=word}}if(line)lines.push(line);return lines}

export default function Home(){
const[step,setStep]=useState<Step>("start"),[name,setName]=useState(""),[category,setCategory]=useState(""),[reviewUrl,setReviewUrl]=useState(""),[website,setWebsite]=useState(""),[address,setAddress]=useState(""),[color,setColor]=useState("#1f6f5f"),[modes,setModes]=useState<Interaction[]>([]),[qr,setQr]=useState(""),[logo,setLogo]=useState(""),[attempted,setAttempted]=useState(false),[busy,setBusy]=useState(false),[businessQuery,setBusinessQuery]=useState(""),[places,setPlaces]=useState<PlaceResult[]>([]),[searching,setSearching]=useState(false),[searchError,setSearchError]=useState(""),[manual,setManual]=useState(false),[selectedPlace,setSelectedPlace]=useState("");
const initials=useMemo(()=>name.split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join("").toUpperCase()||"RK",[name]);
const prompt=promptFor(category),linkStatus=reviewLinkStatus(reviewUrl),ready=!!(name.trim()&&category&&modes.length&&validGoogle(reviewUrl));
function toggle(id:Interaction){setModes(x=>x.includes(id)?x.filter(v=>v!==id):[...x,id])}
function categoryFromTypes(types:string[]){const t=types.join(" ");if(/restaurant|food|cafe|bakery|meal/.test(t))return"Restaurant / Food";if(/beauty|hair|spa|barber/.test(t))return"Salon / Barber / Beauty";if(/dentist|doctor|hospital|health|medical/.test(t))return"Healthcare / Dental";if(/store|shop|retail/.test(t))return"Retail";if(/contractor|plumber|electrician|roof|landscap|moving|cleaning/.test(t))return"Home Service / Contractor";return"Professional Service"}
async function searchBusinesses(){const q=businessQuery.trim();if(q.length<3)return;setSearching(true);setSearchError("");setPlaces([]);try{const r=await fetch("/api/business-search",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query:q})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Search failed.");setPlaces(d.places||[]);if(!(d.places||[]).length)setSearchError("No matching businesses found. Try adding the city or street address.")}catch(e:any){setSearchError(e.message||"Business search is unavailable.")}finally{setSearching(false)}}
function chooseBusiness(p:PlaceResult){setSelectedPlace(p.id);setName(p.name);setAddress(p.address==="Service-area business"?"":p.address);if(p.website)setWebsite(p.website);setCategory(categoryFromTypes(p.types));setReviewUrl("https://search.google.com/local/writereview?placeid="+encodeURIComponent(p.id));setPlaces([]);setManual(false)}
function loadLogo(file?:File){if(!file)return;if(file.size>4_000_000)return alert("Please use a logo under 4 MB.");const r=new FileReader();r.onload=()=>setLogo(String(r.result));r.readAsDataURL(file)}
async function makePreview(){setAttempted(true);if(!ready)return;setQr(await QRCode.toDataURL(reviewUrl.trim(),{width:1400,margin:4,errorCorrectionLevel:"M",color:{dark:"#000000",light:"#FFFFFF"}}));setStep("preview")}
async function newPdf(w:number,h:number){const pdf=await PDFDocument.create();const page=pdf.addPage([w,h]);return{pdf,page,regular:await pdf.embedFont(StandardFonts.Helvetica),bold:await pdf.embedFont(StandardFonts.HelveticaBold)}}
async function drawLogo(pdf:PDFDocument,page:any,x:number,y:number,w:number,h:number){if(!logo)return false;try{const b=await pngBytes(logo);const img=logo.startsWith("data:image/png")?await pdf.embedPng(b):await pdf.embedJpg(b);const s=img.scaleToFit(w,h);page.drawImage(img,{x:x+(w-s.width)/2,y:y+(h-s.height)/2,width:s.width,height:s.height});return true}catch{return false}}
async function cardPdf(){const{pdf,page,regular,bold}=await newPdf(252,144);const brand=hexRgb(color);page.drawRectangle({x:0,y:0,width:160,height:144,color:brand});page.drawRectangle({x:160,y:0,width:92,height:144,color:rgb(1,1,1)});if(!(await drawLogo(pdf,page,14,112,24,20))){page.drawCircle({x:25,y:122,size:10,color:rgb(1,1,1)});page.drawText(initials,{x:25-bold.widthOfTextAtSize(initials,6)/2,y:120,size:6,font:bold,color:brand})}const n=name.trim().slice(0,50);page.drawText(n,{x:42,y:119,size:n.length>30?5.8:7,font:bold,color:rgb(1,1,1),maxWidth:108});page.drawText("YOUR FEEDBACK MATTERS",{x:14,y:88,size:4.5,font:bold,color:rgb(.86,.95,.92)});const lines=wrap(prompt,bold,13.5,132).slice(0,2);lines.forEach((l,i)=>page.drawText(l,{x:14,y:70-i*14,size:13.5,font:bold,color:rgb(1,1,1)}));page.drawText("Share your experience with us on Google.",{x:14,y:lines.length>1?39:51,size:6,font:regular,color:rgb(1,1,1)});page.drawText("Thank you — your feedback helps our business grow.",{x:14,y:13,size:4.3,font:regular,color:rgb(.9,.97,.95)});const qi=await pdf.embedPng(await pngBytes(qr));page.drawImage(qi,{x:171,y:49,width:70,height:70});const s="Scan to review";page.drawText(s,{x:206-bold.widthOfTextAtSize(s,6.3)/2,y:38,size:6.3,font:bold,color:rgb(.08,.1,.09)});return pdf.save()}
async function signPdf(w:number,h:number,title:string,sub:string){const{pdf,page,regular,bold}=await newPdf(w,h);const brand=hexRgb(color);page.drawRectangle({x:0,y:0,width:w,height:h,color:rgb(1,1,1)});try{const photo=await fetch(categoryPhoto(category,1)).then(r=>{if(!r.ok)throw new Error();return r.arrayBuffer()});const img=await pdf.embedJpg(photo);const targetH=h*.31,scale=Math.max(w/img.width,targetH/img.height);page.drawImage(img,{x:(w-img.width*scale)/2,y:h-targetH,width:img.width*scale,height:img.height*scale});page.drawRectangle({x:0,y:h-targetH,width:w,height:targetH,color:brand,opacity:.42})}catch{page.drawRectangle({x:0,y:h-h*.31,width:w,height:h*.31,color:brand})}page.drawRectangle({x:0,y:h-18,width:w,height:18,color:brand});if(!(await drawLogo(pdf,page,w*.12,h-78,w*.76,42))){page.drawText(name,{x:28,y:h-58,size:Math.min(20,Math.max(11,240/name.length)),font:bold,color:rgb(.08,.1,.09),maxWidth:w-56})}const tl=wrap(title,bold,Math.min(28,w/14),w-56).slice(0,2);tl.forEach((l,i)=>page.drawText(l,{x:28,y:h-112-i*30,size:Math.min(28,w/14),font:bold,color:rgb(.08,.1,.09)}));page.drawText(sub,{x:28,y:h-170,size:10,font:regular,color:rgb(.35,.39,.36),maxWidth:w-56});const qi=await pdf.embedPng(await pngBytes(qr));const q=Math.min(w*.42,h*.38);page.drawImage(qi,{x:(w-q)/2,y:55,width:q,height:q});const s="SCAN TO LEAVE A GOOGLE REVIEW";page.drawText(s,{x:(w-bold.widthOfTextAtSize(s,9))/2,y:35,size:9,font:bold,color:brand});return pdf.save()}
async function letterSheet(){const{pdf,page,bold}=await newPdf(612,792);const bytes=await cardPdf();const src=await PDFDocument.load(bytes);const embedded=await pdf.embedPage(src.getPage(0));const positions=[[54,540],[306,540],[54,396],[306,396],[54,252],[306,252],[54,108],[306,108]];for(const[x,y]of positions){page.drawPage(embedded,{x,y,width:252,height:144});page.drawRectangle({x,y,width:252,height:144,borderWidth:.35,borderColor:rgb(.72,.72,.72)})}page.drawText("PRINT AT 100% / ACTUAL SIZE • 8 CARDS • TRIM ON CARD EDGES",{x:54,y:72,size:7.5,font:bold,color:rgb(.35,.39,.36)});return pdf.save()}
async function phonePng(){const canvas=document.createElement("canvas");canvas.width=1080;canvas.height=1920;const x=canvas.getContext("2d")!;x.fillStyle="#f6f4ef";x.fillRect(0,0,1080,1920);try{const photo=new Image();photo.crossOrigin="anonymous";await new Promise<void>((res,rej)=>{photo.onload=()=>res();photo.onerror=()=>rej();photo.src=categoryPhoto(category,0)});const sc=Math.max(1080/photo.width,760/photo.height);x.drawImage(photo,(1080-photo.width*sc)/2,0,photo.width*sc,photo.height*sc);x.globalAlpha=.58;x.fillStyle=color;x.fillRect(0,0,1080,760);x.globalAlpha=1}catch{x.fillStyle=color;x.fillRect(0,0,1080,760)}x.fillStyle="#fff";x.textAlign="center";x.font="700 48px Arial";x.fillText(name,540,190,900);x.font="800 86px Arial";const lines=prompt.length>22?[prompt.slice(0,prompt.lastIndexOf(" ",22)),prompt.slice(prompt.lastIndexOf(" ",22)+1)]:[prompt];lines.forEach((l,i)=>x.fillText(l,540,500+i*95,920));x.font="36px Arial";x.fillText("Share your experience with us on Google.",540,690,900);x.fillStyle="#fff";x.shadowColor="rgba(0,0,0,.12)";x.shadowBlur=32;x.fillRect(220,790,640,690);x.shadowBlur=0;const im=new Image();await new Promise<void>((res,rej)=>{im.onload=()=>res();im.onerror=rej;im.src=qr});x.fillStyle="#fff";x.fillRect(285,850,510,510);x.drawImage(im,315,880,450,450);x.fillStyle="#17201b";x.font="700 38px Arial";x.fillText("Scan to review",540,1420);x.font="28px Arial";x.fillStyle="#667069";x.fillText("Open your camera and point it at the QR code",540,1535,760);return new Promise<Blob>((res,rej)=>canvas.toBlob(b=>b?res(b):rej(),"image/png"))}
async function templatesPdf(){const{pdf,page,regular,bold}=await newPdf(612,792);const brand=hexRgb(color);page.drawRectangle({x:0,y:718,width:612,height:74,color:brand});page.drawText("REVIEW REQUEST TEMPLATES",{x:42,y:748,size:19,font:bold,color:rgb(1,1,1)});page.drawText(name,{x:42,y:690,size:16,font:bold,color:rgb(.08,.1,.09),maxWidth:520});const blocks=[["TEXT MESSAGE",`Hi [First name] — thanks for choosing ${name}. If you have a moment, we'd appreciate your feedback on Google: ${reviewUrl}`],["EMAIL",`Subject: How was your experience with ${name}?\n\nHi [First name], thank you for choosing ${name}. Your feedback helps us improve and helps other customers find us. Share your experience here: ${reviewUrl}`],["FOLLOW-UP",`Hi [First name] — just a quick follow-up from ${name}. If you'd like to share feedback, here's our Google review link: ${reviewUrl}`],["INVOICE / RECEIPT",`We value your feedback. Scan our review QR or visit: ${reviewUrl}`]];let y=640;for(const[t,b]of blocks){page.drawText(t,{x:42,y,size:10,font:bold,color:brand});y-=18;for(const para of b.split("\n")){for(const line of wrap(para,regular,9.5,520)){page.drawText(line,{x:42,y,size:9.5,font:regular,color:rgb(.2,.23,.21)});y-=13}y-=4}y-=18}return pdf.save()}
async function digitalPng(w:number,h:number,title:string){const c=document.createElement("canvas");c.width=w;c.height=h;const x=c.getContext("2d")!;x.fillStyle="#f6f4ef";x.fillRect(0,0,w,h);x.fillStyle=color;x.fillRect(0,0,Math.round(w*.43),h);x.fillStyle="#fff";x.textAlign="left";x.font=`700 ${Math.round(h*.08)}px Arial`;const words=title.split(" ");let line="",yy=h*.28;for(const word of words){const test=line?line+" "+word:word;if(x.measureText(test).width<w*.35)line=test;else{x.fillText(line,w*.055,yy);yy+=h*.1;line=word}}if(line)x.fillText(line,w*.055,yy);x.font=`600 ${Math.round(h*.032)}px Arial`;x.fillText(name,w*.055,h*.82,w*.32);const im=new Image();await new Promise<void>((res,rej)=>{im.onload=()=>res();im.onerror=rej;im.src=qr});const q=Math.min(h*.58,w*.31);x.fillStyle="#fff";x.fillRect(w*.57-q*.08,(h-q)/2-q*.08,q*1.16,q*1.16);x.drawImage(im,w*.57,(h-q)/2,q,q);x.fillStyle="#17201b";x.textAlign="center";x.font=`700 ${Math.round(h*.035)}px Arial`;x.fillText("Scan to review",w*.57+q/2,(h+q)/2+h*.08);return new Promise<Blob>((res,rej)=>c.toBlob(b=>b?res(b):rej(),"image/png"))}
async function invoiceQrPng(){const c=document.createElement("canvas");c.width=1200;c.height=500;const x=c.getContext("2d")!;x.fillStyle="#fff";x.fillRect(0,0,1200,500);x.fillStyle=color;x.fillRect(0,0,520,500);x.fillStyle="#fff";x.font="700 52px Arial";x.fillText("How did we do?",58,145,400);x.font="30px Arial";x.fillText("Your feedback helps our",58,215,400);x.fillText("business grow.",58,255,400);x.font="700 25px Arial";x.fillText(name,58,405,400);const im=new Image();await new Promise<void>((res,rej)=>{im.onload=()=>res();im.onerror=rej;im.src=qr});x.drawImage(im,700,55,350,350);x.fillStyle="#17201b";x.textAlign="center";x.font="700 24px Arial";x.fillText("SCAN TO REVIEW",875,450);return new Promise<Blob>((res,rej)=>c.toBlob(b=>b?res(b):rej(),"image/png"))}
function templates(){return `REVIEW REQUEST TEMPLATES — ${name}\n\nTEXT MESSAGE\nHi [First name] — thanks for choosing ${name}. If you have a moment, we'd appreciate your feedback on Google: ${reviewUrl}\n\nEMAIL\nSubject: How was your experience with ${name}?\n\nHi [First name],\nThank you for choosing ${name}. Your feedback helps us improve and helps other customers find us. If you'd like to share your experience, you can leave a Google review here:\n${reviewUrl}\n\nThank you,\n${name}\n\nFOLLOW-UP\nHi [First name] — just a quick follow-up from ${name}. If you'd like to share feedback about your experience, here's our Google review link: ${reviewUrl}\n\nINVOICE / RECEIPT LINE\nWe value your feedback. Scan our review QR or visit: ${reviewUrl}\n`}
async function guidePdf(){const{pdf,page,regular,bold}=await newPdf(612,792);const brand=hexRgb(color);page.drawRectangle({x:0,y:720,width:612,height:72,color:brand});page.drawText("YOUR REVIEW KIT",{x:42,y:746,size:22,font:bold,color:rgb(1,1,1)});page.drawText(name,{x:42,y:695,size:18,font:bold,color:rgb(.08,.1,.09),maxWidth:520});const rows:string[][]=[["Review Card","Hand it to customers after a completed visit, service, or purchase."],["8-up Print Sheet","Print at 100% / Actual Size on US Letter, then trim on card edges."],["Phone Graphic","Share digitally or display on a phone or tablet."],["Standalone QR","Use anywhere you need a direct path to your Google review flow."],["Request Templates","Use the PDF or TXT copy as a starting point, then personalize it."]];if(modes.includes("visit-us"))rows.push(["5×7 Counter Sign","Place at a checkout, reception desk, table, or front counter."]);if(modes.includes("we-visit"))rows.push(["4×6 Leave-Behind","Leave it after the job, or include it with paperwork."]);if(modes.includes("delivery"))rows.push(["4×6 Package Insert","Include it with delivered orders or bags."]);if(modes.includes("online"))rows.push(["Digital Review Card","Use it in digital follow-up after remote work."]);rows.push(["Before you use the kit","Scan the downloaded QR with a phone and confirm it opens the review-writing flow."],["Important","Ask for honest feedback. Do not offer rewards or only ask customers likely to leave positive reviews."]);let y=640;for(const[r,d]of rows){page.drawText(r,{x:42,y,size:12,font:bold,color:brand});for(const line of wrap(d,regular,9.5,500)){y-=14;page.drawText(line,{x:42,y,size:9.5,font:regular,color:rgb(.25,.29,.26)})}y-=20}return pdf.save()}
async function buildKit(){if(!qr)return;setBusy(true);try{const zip=new JSZip(),base=slug(name);zip.file(base+"-review-card-3.5x2.pdf",await cardPdf());zip.file(base+"-print-sheet-letter.pdf",await letterSheet());zip.file(base+"-phone-review-1080x1920.png",await phonePng());zip.file(base+"-qr-code.png",await pngBytes(qr));zip.file(base+"-review-request-templates.txt",templates());zip.file(base+"-review-request-templates.pdf",await templatesPdf());zip.file(base+"-start-here.pdf",await guidePdf());if(modes.includes("visit-us"))zip.file(base+"-counter-sign-5x7.pdf",await signPdf(360,504,prompt,"Scan the QR code to share your experience."));if(modes.includes("we-visit")){zip.file(base+"-leave-behind-4x6.pdf",await signPdf(288,432,prompt,"Thanks for trusting us with your project."));zip.file(base+"-invoice-estimate-review-qr.png",await invoiceQrPng())}if(modes.includes("delivery"))zip.file(base+"-package-insert-4x6.pdf",await signPdf(288,432,"Thanks for your order","We'd love to hear about your experience."));if(modes.includes("online"))zip.file(base+"-email-web-review-1200x628.png",await digitalPng(1200,628,prompt));const blob=await zip.generateAsync({type:"blob"});download(blob,base+"-review-kit.zip","application/zip")}finally{setBusy(false)}}

return <main><header className="topbar"><button className="brandBtn" onClick={()=>setStep("start")}>Review Kit</button><div className="pill">$29 one-time <span>•</span> no subscription</div></header>
{step==="start"&&<section className="hero"><div className="eyebrow">GOOGLE REVIEW MATERIALS, DONE FOR YOU</div><h1>Make it easier for happy customers to leave a review.</h1><p className="sub">Create a polished, branded review kit with QR cards, signs, graphics and ready-to-send request templates.</p><button className="primary big" onClick={()=>setStep("setup")}>Create my review kit →</button><div className="miniTrust"><span>One-time purchase</span><span>Preview before buying</span><span>No design skills needed</span></div><div className="included"><div><b>What you get</b><p>Review card • Print sheet • Phone graphic • QR code • Request templates • Start guide</p></div><div><b>Built for your workflow</b><p>Counter sign, leave-behind, package insert or digital asset added automatically.</p></div></div></section>}
{step==="setup"&&<section className="builderWrap"><div className="builder"><button className="back" onClick={()=>setStep("start")}>← Back</button><div className="eyebrow">BUSINESS SETUP</div><h1>Build your kit.</h1><p className="sub small">Add your business details. We'll adapt the materials to how you work with customers.</p><div className="businessFinder"><label>Find your business *<div className="searchRow"><input value={businessQuery} onChange={e=>setBusinessQuery(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"){e.preventDefault();searchBusinesses()}}} placeholder="Business name + city or address"/><button type="button" className="primary searchBtn" disabled={searching} onClick={searchBusinesses}>{searching?"Searching…":"Search"}</button></div></label>{searchError&&<div className="searchError">{searchError}</div>}{places.length>0&&<div className="placeResults">{places.map(p=><button type="button" className="placeResult" key={p.id} onClick={()=>chooseBusiness(p)}><strong>{p.name}</strong><span>{p.address}</span><small>Select this business →</small></button>)}</div>}{selectedPlace&&!manual&&<div className="selectedBusiness"><span className="selectedCheck">✓</span><div><strong>{name}</strong><span>{address||"Service-area business"}</span><small>Google business selected • review destination connected</small></div></div>}<button type="button" className="textButton" onClick={()=>{setManual(x=>!x);setSelectedPlace("");setReviewUrl("")}}>{manual?"← Use business search":"Can't find it? Enter details manually"}</button></div><div className="formGrid">{manual&&<label>Business name *<input value={name} maxLength={120} onChange={e=>setName(e.target.value)} placeholder="Your business name"/></label>}<label>Business category *<select value={category} onChange={e=>setCategory(e.target.value)}><option value="">Choose a category</option>{cats.map(c=><option key={c}>{c}</option>)}</select></label><fieldset className="full interactionField"><legend>How do you work with customers? *</legend><p className="helper">Choose all that apply.</p><div className="interactionGrid">{interactions.map(o=><button type="button" key={o.id} className={modes.includes(o.id)?"interaction selected":"interaction"} onClick={()=>toggle(o.id)}><span className="check">{modes.includes(o.id)?"✓":""}</span><span><strong>{o.title}</strong><small>{o.detail}</small></span></button>)}</div></fieldset>{manual&&<label className="full">Google review link *<input value={reviewUrl} onChange={e=>setReviewUrl(e.target.value)} placeholder="Paste your direct Google review link"/><span className="helper">Only needed for manual setup. Use the direct link from your Google Business Profile’s “Ask for reviews” option.</span>{reviewUrl&&linkStatus==="search"&&<span className="error">This is a Google search link, not a direct review link.</span>}{reviewUrl&&linkStatus==="invalid"&&<span className="error">Paste a direct Google review link.</span>}</label>}<label>Website <span className="optional">Optional</span><input value={website} onChange={e=>setWebsite(e.target.value)} placeholder="yourbusiness.com"/></label><label>Location <span className="optional">Optional</span><input value={address} onChange={e=>setAddress(e.target.value)} placeholder="City, State or full address"/></label><label>Brand color <span className="optional">Optional</span><div className="colorRow"><input className="colorInput" type="color" value={color} onChange={e=>setColor(e.target.value)}/><span>{color.toUpperCase()}</span></div></label><label>Logo <span className="optional">Optional</span><input type="file" accept="image/png,image/jpeg" onChange={e=>loadLogo(e.target.files?.[0])}/><span className="helper">{logo?"Logo ready.":"PNG or JPG, up to 4 MB. No logo? We'll use your initials."}</span></label></div>{attempted&&!ready&&<div className="formError">Complete the required fields above.</div>}<button className="primary" onClick={makePreview}>Create my preview →</button></div></section>}
{step==="preview"&&<section className="previewWrap"><div className="previewHead"><button className="back" onClick={()=>setStep("setup")}>← Edit details</button><div><div className="eyebrow">YOUR REVIEW KIT</div><h1>{name}</h1></div></div><div className="previewGrid"><div className="reviewCard premiumCard" style={{"--brand":color,"--photo":`url(${categoryPhoto(category,0)})`} as React.CSSProperties}><div className="reviewCardBrand"><div className="reviewIdentity">{logo?<img className="logoImage" src={logo} alt="Business logo"/>:<div className="logoDot">{initials}</div>}<div className="assetTitle">{name}</div></div><div className="reviewMessage"><div className="reviewKicker">YOUR FEEDBACK MATTERS</div><h2>{prompt}</h2><p>Share your experience with us on Google.</p></div><div className="reviewThanks">Thank you — your feedback helps our business grow.</div><div className="photoCreditLine">PERSONALIZED FOR YOUR BUSINESS</div></div><div className="reviewCardQr">{qr&&<img src={qr} alt={"QR code for "+name}/>}<strong>Scan to review</strong><span>Open your camera</span></div></div><aside className="detailsPanel"><div className="eyebrow">PERSONALIZED & READY</div><h3>Your kit is ready.</h3><div className="kitCount">7 core assets + {modes.length} workflow {modes.length===1?"asset":"assets"}</div><div className="assetList"><span>✓ Review Card PDF</span><span>✓ US Letter Print Sheet</span><span>✓ 1080×1920 Phone Graphic</span><span>✓ High-resolution QR PNG</span><span>✓ Review Request Templates PDF + TXT</span><span>✓ Start Here Guide</span>{modes.includes("visit-us")&&<span>✓ 5×7 Counter Sign</span>}{modes.includes("we-visit")&&<><span>✓ 4×6 Leave-Behind</span><span>✓ Invoice / Estimate QR Graphic</span></>}{modes.includes("delivery")&&<span>✓ 4×6 Package Insert</span>}{modes.includes("online")&&<span>✓ Email / Web Review Graphic</span>}</div><button className="primary fullBtn" disabled={busy} onClick={buildKit}>{busy?"Building your kit…":"Download complete kit (.zip)"}</button><button className="secondary fullBtn" onClick={async()=>download(await cardPdf(),slug(name)+"-review-card.pdf")}>Download review card only</button><p className="fine">Before using printed materials, scan the QR from the downloaded file with your phone to confirm it opens the correct Google review page.</p></aside></div></section>}</main>
}